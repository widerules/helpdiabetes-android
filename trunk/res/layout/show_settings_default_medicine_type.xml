<?xml version="1.0" encoding="utf-8"?>
<!-- Please read info.txt for license and legal information -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/linearLayoutVerticalWithPadding"
    android:background="@drawable/bg" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

          <TextView
            android:id="@+id/textView2"
            style="@style/defaultFontTitle"
            android:text="@string/pref_medicine_types"
            android:layout_weight="1.0" />
        
        <Button
            android:id="@+id/buttonNext"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/ic_menu_forward"
            android:visibility="gone" />
        
    </LinearLayout>

    <TextView style="@style/defaultFontNormal" android:text="@string/select_standard_medicine_type" />
    
    <ListView
        android:id="@+android:id/list"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:cacheColorHint="#00000000"
        android:layout_weight="1.0" />

    <TextView style="@style/defaultFontNormal" android:text="@string/standard_is_used_for"/>
    
</LinearLayout>